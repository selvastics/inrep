% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/platform_deployment.R
\name{prepare_shinyapps_deploy}
\alias{prepare_shinyapps_deploy}
\title{Prepare a study script for deployment to shinyapps.io}
\usage{
prepare_shinyapps_deploy(
  study_script,
  output_dir = NULL,
  app_name = NULL,
  deploy = FALSE,
  account = NULL
)
}
\arguments{
\item{study_script}{Path to the study R script (e.g. \code{"my_study.R"}).}

\item{output_dir}{Directory to create. Defaults to \code{"deploy"} next to the script.}

\item{app_name}{Optional application name for shinyapps.io.}

\item{deploy}{Logical. If \code{TRUE}, calls \code{rsconnect::deployApp()}
after creating the folder. Default \code{FALSE}.}

\item{account}{Optional shinyapps.io account name.}
}
\value{
Invisibly returns the path to the deployment folder.
}
\description{
Creates a deployment-ready folder from a study R script.
The folder contains an \code{app.R} wrapper and can be deployed directly
with \code{rsconnect::deployApp()}.
}
\details{
The function creates a minimal deployment folder containing:
\itemize{
  \item \code{app.R} -- sources the study script via \code{inrep::launch_study()}
  \item A copy of the original study script
  \item A \code{.rscignore} to exclude unnecessary files
}

For shinyapps.io to install \code{inrep} automatically, the GitHub repository
(\url{https://github.com/selvastics/inrep}) must be public.
}
\examples{
\dontrun{
# Prepare deployment folder (without deploying)
prepare_shinyapps_deploy("case_studies/my_study.R")

# Prepare and deploy in one step
prepare_shinyapps_deploy("case_studies/my_study.R", deploy = TRUE)
}
}
